# Leave empty in development to use Vite's proxy (see vite.config.js) and same-origin /api/* requests.
# For production builds, set the full backend origin, e.g. https://api.example.com
VITE_API_BASE_URL=
