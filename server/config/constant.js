const MEMBER_STATUS = {
    PENDING: "Pending",
    ACCEPTED: "Accepted",
    REJECTED: "Rejected",
}

const USER_ROLE = {
    ADMIN: "admin",
    CITIZEN: "citizen",
    ORGANIZATION: "organization",
}

module.exports = {
    MEMBER_STATUS,
    USER_ROLE,
}